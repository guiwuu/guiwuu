# mysql notes

```
mysql
	basic
		 mysql -u foo -p -h 127.0.0.1
	status
		show status like "%thread%";
	database
		show databases;
	variables
		show variables  like "%mem%"
		show variables  like "%time%"
```