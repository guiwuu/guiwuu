# bash notes

```
Notes of Bash
	if
		Simple If
			if..then..fi
		If-Else
			if..then..else..fi
		Else If ladder
			if..elif..else..fi
		Nested if
			if..then..else..if..then..fi..fi..
		doc
			http://www.thegeekstuff.com/2010/06/bash-if-statement-examples
	condition
		LIKE
			[[ $HOSTNAME =~ sqa$ ]]
		OR
			[[ $HOSTNAME =~ sqa$ ]] || [[ $HOSTNAME =~ pre. ]]
```